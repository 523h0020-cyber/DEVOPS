
# Cách sử dụng scripts để deploy lên AWS

cd "$(dirname "$0")" # Navigate to scripts folder

 "🚀 HƯỚNG DẪN DEPLOYMENT LÊN AWS"
 "=================================="
 ""
 "Trước khi chạy các scripts:"
 "1. Đảm bảo bạn có AWS EC2 instance chạy Ubuntu 24.04 LTS"
 "2. SSH vào instance với user có quyền sudo"
 "3. Clone repo: git clone https://github.com/523h0020-cyber/DEVOPS.git"
 ""
 "Cách 1: TỰ ĐỘNG (Khuyến nghị)"
 "================================"
 "Chạy một lệnh duy nhất:"
 ""
 "  sudo bash deploy-to-aws.sh"
 ""
 "Script này sẽ tự động thực hiện:"
 "  ✅ Phase 1: Cài đặt Node.js, PM2, Nginx"
 "  ✅ Phase 2: Cài MongoDB, deploy ứng dụng"
 "  ✅ Phase 3: Cấu hình SSL & domain 523h0020.site"
 "  ✅ Bật MongoDB backup hàng ngày"
 ""
 "Cách 2: TỪNG BƯỚC"
 "================================"
 "Nếu muốn kiểm soát từng bước:"
 ""
 "1. Cài đặt môi trường:"
 "   sudo bash setup.sh"
 ""
 "2. Deploy ứng dụng & MongoDB:"
 "   sudo bash phase2.sh"
 ""
 "3. Cấu hình SSL & domain:"
 "   sudo bash phase3-ssl-setup.sh"
 ""
 "4. Bật backup MongoDB:"
 "   sudo bash -c 'cp backup-mongodb.sh /usr/local/bin/ && chmod +x /usr/local/bin/backup-mongodb.sh'"
 "   sudo crontab -e  # Thêm dòng: 0 2 * * * /usr/local/bin/backup-mongodb.sh"
 ""
 "QUẢN LÝ ỨNG DỤNG"
 "================================"
 "Xem trạng thái:        pm2 status"
 "Xem logs:              pm2 logs midterm-app"
 "Restart ứng dụng:      pm2 restart midterm-app"
 "Stop ứng dụng:         pm2 stop midterm-app"
 "Backup database:       sudo bash backup-mongodb.sh"
 "Restore backup:        sudo bash restore-mongodb.sh /path/to/backup.tar.gz"
 "Health check:          sudo bash health-check.sh"
 ""
 "YÊU CẦU"
 "================================"
 "✅ Port 3000: Node.js app"
 "✅ Port 80/443: Nginx reverse proxy"
 "✅ MongoDB: Lưu trữ dữ liệu (không mất khi tắt app)"
 "✅ SSL: HTTPS với Let's Encrypt"
 "✅ Backup: Hàng ngày lúc 2 AM"
 "✅ PM2: Tự động restart app nếu crash"
 ""
